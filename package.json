{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./frontend\" --filter \"./backend\" --if-present run typecheck",
    "dev:frontend": "pnpm --filter @workspace/sabaytenh run dev",
    "dev:backend": "pnpm --filter @workspace/api-server run dev",
    "dev": "pnpm run dev:backend & pnpm run dev:frontend",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:setup": "pnpm --filter @workspace/db run setup"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-darwin-arm64": "^4.60.4",
    "@tailwindcss/oxide-darwin-arm64": "^4.3.0",
    "@types/node": "catalog:",
    "lightningcss": "^1.32.0",
    "lightningcss-darwin-arm64": "^1.32.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "bcrypt": "^6.0.0"
  }
}
